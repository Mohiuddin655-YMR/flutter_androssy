name: androssy_widgets
description: Collection of widget with advanced style and controlling system.
version: 0.0.2
homepage: https://github.com/P-YMR/flutter_androssy.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.2.3
  flutter_svg: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: