name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # CUSTOMS
  app_color: ^0.0.7
  flutter_app_navigator: ^1.0.1

  # OTHERS
  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  shared_preferences: ^2.2.2

  flutter_andomie: ^0.5.9
  flutter_androssy:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.2

flutter:

  uses-material-design: true

  assets:
    - assets/contents/
    - assets/icons/
    - assets/images/